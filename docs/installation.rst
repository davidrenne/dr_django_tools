============
Installation
============

At the command line::

    $ easy_install dr-django-tools

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dr-django-tools
    $ pip install dr-django-tools
